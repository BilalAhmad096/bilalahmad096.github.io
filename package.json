{
  "name": "mintorian-research-assistant",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Ask Mintorian research and collaboration assistant for mintorian.com",
  "scripts": {
    "dev:api": "wrangler dev --config worker/wrangler.jsonc --port 8787",
    "deploy:api": "wrangler deploy --config worker/wrangler.jsonc",
    "check": "node --check js/backtotop.js && node --check js/assistant-loader.js && node --check js/ask-mintorian.js && node --check worker/src/index.js && node --check worker/src/email.js && node --check worker/src/knowledge.js && node --check worker/src/openai.js && node --check worker/src/security.js && node --check tests/live-agent-eval.mjs",
    "test": "node --test tests/*.test.js",
    "test:live": "node tests/live-agent-eval.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
